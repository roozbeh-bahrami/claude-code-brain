# <PROJECT NAME> — Claude project context (LEAN)

> This repo is the **<project>** project. Shared knowledge lives in `<path-to-your-knowledge-base>/` and loads ON DEMAND — keep this file tiny.

## Session start
Read `<knowledge-base>/rituals/session-open.md` and run it. Project state: `projects/<name>/STATE.md`. Domain knowledge: open per START-HERE's index, only what today's task needs.

## This project (purpose)
<2-3 sentences: what this project IS, what "done" means, what is explicitly OUT of scope.>

## Project-specific HARD RULES (binding)
1. <the live system / data that must never be touched, with its ID>
2. <where all work happens instead (clone / staging / branch)>
3. <credentials rule: which .env, what never gets printed>
4. <verification rule: what proof "done" requires>
